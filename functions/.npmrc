# Cloud Build rejects lockfile entries published within the last 24h. Resolve
# the same way locally so we never commit a lockfile the deploy will reject.
# Kept out of the upload (.gcloudignore) so it cannot override the builder's
# own policy with a weaker value.
minimum-release-age=1440
